# UK-AIR SOS system documentation moved

The authoritative UK-AIR SOS system documentation has moved to:

[`TEST-uk-aq/uk-aq-ops/system_docs/ingest/sos/`](https://github.com/TEST-uk-aq/uk-aq-ops/tree/main/system_docs/ingest/sos)

Use the Ops reading order and contract there for current polling, Cloud Run, interface, operations and validation behaviour.

This file is retained only as a compatibility pointer for existing links. It is not an editable behavioural authority.

Runtime implementation remains in this `uk-aq-ingest` repository. Canonical database SQL remains in `uk-aq-schema`.
